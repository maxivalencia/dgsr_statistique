{% extends 'base.html.twig' %}

{% block title %}CtCarosserie{% endblock %}

{% block body %}
    <h1>Carosserie</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_carosserie.id }}</td>
            </tr>
            <tr>
                <th>Carosserie Libelle</th>
                <td>{{ ct_carosserie.crsLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_carosserie_index') }}">Retour</a>

    <a href="{{ path('ct_carosserie_edit', {'id': ct_carosserie.id}) }}">Modifier</a>

    {{ include('ct_carosserie/_delete_form.html.twig') }}
{% endblock %}
