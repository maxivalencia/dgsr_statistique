{% extends 'base.html.twig' %}

{% block title %}CtCarosserie{% endblock %}

{% block body %}
    <h1>CtCarosserie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_carosserie.id }}</td>
            </tr>
            <tr>
                <th>CrsLibelle</th>
                <td>{{ ct_carosserie.crsLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_carosserie_index') }}">back to list</a>

    <a href="{{ path('ct_carosserie_edit', {'id': ct_carosserie.id}) }}">edit</a>

    {{ include('ct_carosserie/_delete_form.html.twig') }}
{% endblock %}
