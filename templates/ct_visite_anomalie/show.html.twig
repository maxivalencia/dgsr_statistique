{% extends 'base.html.twig' %}

{% block title %}CtVisiteAnomalie{% endblock %}

{% block body %}
    <h1>CtVisiteAnomalie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_visite_anomalie.id }}</td>
            </tr>
            <tr>
                <th>CtAnomalieId</th>
                <td>{{ ct_visite_anomalie.ctAnomalieId }}</td>
            </tr>
            <tr>
                <th>CtVisiteId</th>
                <td>{{ ct_visite_anomalie.ctVisiteId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_visite_anomalie_index') }}">back to list</a>

    <a href="{{ path('ct_visite_anomalie_edit', {'id': ct_visite_anomalie.id}) }}">edit</a>

    {{ include('ct_visite_anomalie/_delete_form.html.twig') }}
{% endblock %}
