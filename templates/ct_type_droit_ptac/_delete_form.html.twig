<form method="post" action="{{ path('ct_type_droit_ptac_delete', {'id': ct_type_droit_ptac.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet element?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_type_droit_ptac.id) }}">
    <button class="btn">Supprimer</button>
</form>
