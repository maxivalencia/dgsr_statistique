{% extends 'base.html.twig' %}

{% block title %}CtTypeDroitPtac{% endblock %}

{% block body %}
    <h1>Type Droit PTAC</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_type_droit_ptac.id }}</td>
            </tr>
            <tr>
                <th>Type Droit ptac Libelle</th>
                <td>{{ ct_type_droit_ptac.tpDpLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_type_droit_ptac_index') }}">Retour</a>

    <a href="{{ path('ct_type_droit_ptac_edit', {'id': ct_type_droit_ptac.id}) }}">Modifier</a>

    {{ include('ct_type_droit_ptac/_delete_form.html.twig') }}
{% endblock %}
