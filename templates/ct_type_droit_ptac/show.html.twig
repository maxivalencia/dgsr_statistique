{% extends 'base.html.twig' %}

{% block title %}CtTypeDroitPtac{% endblock %}

{% block body %}
    <h1>CtTypeDroitPtac</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_type_droit_ptac.id }}</td>
            </tr>
            <tr>
                <th>TpDpLibelle</th>
                <td>{{ ct_type_droit_ptac.tpDpLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_type_droit_ptac_index') }}">back to list</a>

    <a href="{{ path('ct_type_droit_ptac_edit', {'id': ct_type_droit_ptac.id}) }}">edit</a>

    {{ include('ct_type_droit_ptac/_delete_form.html.twig') }}
{% endblock %}
