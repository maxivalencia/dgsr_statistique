{% extends 'base.html.twig' %}

{% block title %}CtTypeDroitPtac index{% endblock %}

{% block body %}
    <h1>Liste Type Droit PTAC</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle Type Droit Ptac</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_type_droit_ptac in ct_type_droit_ptacs %}
            <tr>
                <td>{{ ct_type_droit_ptac.id }}</td>
                <td>{{ ct_type_droit_ptac.tpDpLibelle }}</td>
                <td>
                    <a href="{{ path('ct_type_droit_ptac_show', {'id': ct_type_droit_ptac.id}) }}">voir</a>
                    <a href="{{ path('ct_type_droit_ptac_edit', {'id': ct_type_droit_ptac.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_type_droit_ptac_new') }}">Créer</a>
{% endblock %}
