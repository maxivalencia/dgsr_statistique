{% extends 'base.html.twig' %}

{% block title %}CtTypeDroitPtac index{% endblock %}

{% block body %}
    <h1>CtTypeDroitPtac index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TpDpLibelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_type_droit_ptac in ct_type_droit_ptacs %}
            <tr>
                <td>{{ ct_type_droit_ptac.id }}</td>
                <td>{{ ct_type_droit_ptac.tpDpLibelle }}</td>
                <td>
                    <a href="{{ path('ct_type_droit_ptac_show', {'id': ct_type_droit_ptac.id}) }}">show</a>
                    <a href="{{ path('ct_type_droit_ptac_edit', {'id': ct_type_droit_ptac.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_type_droit_ptac_new') }}">Create new</a>
{% endblock %}
