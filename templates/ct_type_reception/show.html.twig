{% extends 'base.html.twig' %}

{% block title %}CtTypeReception{% endblock %}

{% block body %}
    <h1>CtTypeReception</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_type_reception.id }}</td>
            </tr>
            <tr>
                <th>TprcpLibelle</th>
                <td>{{ ct_type_reception.tprcpLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_type_reception_index') }}">back to list</a>

    <a href="{{ path('ct_type_reception_edit', {'id': ct_type_reception.id}) }}">edit</a>

    {{ include('ct_type_reception/_delete_form.html.twig') }}
{% endblock %}
