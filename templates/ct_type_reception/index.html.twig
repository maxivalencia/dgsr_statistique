{% extends 'base.html.twig' %}

{% block title %}CtTypeReception index{% endblock %}

{% block body %}
    <h1>CtTypeReception index</h1>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TprcpLibelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_type_reception in ct_type_receptions %}
            <tr>
                <td>{{ ct_type_reception.id }}</td>
                <td>{{ ct_type_reception.tprcpLibelle }}</td>
                <td>
                    <a href="{{ path('ct_type_reception_show', {'id': ct_type_reception.id}) }}">show</a>
                    <a href="{{ path('ct_type_reception_edit', {'id': ct_type_reception.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_type_reception_new') }}">Create new</a>
{% endblock %}
