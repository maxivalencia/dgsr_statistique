<form method="post" action="{{ path('ct_type_reception_delete', {'id': ct_type_reception.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet element?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_type_reception.id) }}">
    <button class="btn">Supprimer</button>
</form>
