{% extends 'base.html.twig' %}

{% block title %}CtGenreCategorie{% endblock %}

{% block body %}
    <h1>CtGenreCategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_genre_categorie.id }}</td>
            </tr>
            <tr>
                <th>GcLibelle</th>
                <td>{{ ct_genre_categorie.gcLibelle }}</td>
            </tr>
            <tr>
                <th>GcIsCalculable</th>
                <td>{{ ct_genre_categorie.gcIsCalculable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_genre_categorie_index') }}">back to list</a>

    <a href="{{ path('ct_genre_categorie_edit', {'id': ct_genre_categorie.id}) }}">edit</a>

    {{ include('ct_genre_categorie/_delete_form.html.twig') }}
{% endblock %}
