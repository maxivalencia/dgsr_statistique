{% extends 'base.html.twig' %}

{% block title %}CtMotifTarif{% endblock %}

{% block body %}
    <h1>Motif Tarif</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_motif_tarif.id }}</td>
            </tr>
            <tr>
                <th>Motif Tarif Prix</th>
                <td>{{ ct_motif_tarif.mtfTrfPrix }}</td>
            </tr>
            <tr>
                <th>Motif Tarif Date</th>
                <td>{{ ct_motif_tarif.mtfTrfDate }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_motif_tarif_index') }}">Retour</a>

    <a href="{{ path('ct_motif_tarif_edit', {'id': ct_motif_tarif.id}) }}">Modifier</a>

    {{ include('ct_motif_tarif/_delete_form.html.twig') }}
{% endblock %}
