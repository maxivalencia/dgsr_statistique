{% extends 'base.html.twig' %}

{% block title %}CtMarque index{% endblock %}

{% block body %}
    <h1>Liste Marque</h1>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marque Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_marque in ct_marques %}
            <tr>
                <td>{{ ct_marque.id }}</td>
                <td>{{ ct_marque.mrqLibelle }}</td>
                <td>
                    <a href="{{ path('ct_marque_show', {'id': ct_marque.id}) }}">voir</a>
                    <a href="{{ path('ct_marque_edit', {'id': ct_marque.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_marque_new') }}">Créer</a>
{% endblock %}
