{% extends 'base.html.twig' %}

{% block title %}CtAnomalieType{% endblock %}

{% block body %}
    <h1>Type Anomalie</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_anomalie_type.id }}</td>
            </tr>
            <tr>
                <th>Type Anomalie Libelle</th>
                <td>{{ ct_anomalie_type.atpLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_anomalie_type_index') }}">Retour</a>

    <a href="{{ path('ct_anomalie_type_edit', {'id': ct_anomalie_type.id}) }}">Modifier</a>

    {{ include('ct_anomalie_type/_delete_form.html.twig') }}
{% endblock %}
