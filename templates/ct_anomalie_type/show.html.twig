{% extends 'base.html.twig' %}

{% block title %}CtAnomalieType{% endblock %}

{% block body %}
    <h1>CtAnomalieType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_anomalie_type.id }}</td>
            </tr>
            <tr>
                <th>AtpLibelle</th>
                <td>{{ ct_anomalie_type.atpLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_anomalie_type_index') }}">back to list</a>

    <a href="{{ path('ct_anomalie_type_edit', {'id': ct_anomalie_type.id}) }}">edit</a>

    {{ include('ct_anomalie_type/_delete_form.html.twig') }}
{% endblock %}
