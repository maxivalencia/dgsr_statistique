{% extends 'base.html.twig' %}

{% block title %}CtAnomalieType index{% endblock %}

{% block body %}
    <h1>CtAnomalieType index</h1>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>AtpLibelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_anomalie_type in ct_anomalie_types %}
            <tr>
                <td>{{ ct_anomalie_type.id }}</td>
                <td>{{ ct_anomalie_type.atpLibelle }}</td>
                <td>
                    <a href="{{ path('ct_anomalie_type_show', {'id': ct_anomalie_type.id}) }}">show</a>
                    <a href="{{ path('ct_anomalie_type_edit', {'id': ct_anomalie_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_anomalie_type_new') }}">Create new</a>
{% endblock %}
