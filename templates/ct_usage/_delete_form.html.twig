<form method="post" action="{{ path('ct_usage_delete', {'id': ct_usage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_usage.id) }}">
    <button class="btn">Delete</button>
</form>
