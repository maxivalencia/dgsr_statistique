{% extends 'base.html.twig' %}

{% block title %}CtDroitPtac index{% endblock %}

{% block body %}
    <h1>Liste Droits Poids Total A Charge</h1>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Prix Minimum</th>
                <th>Prix Maximum</th>
                <th>Droit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_droit_ptac in ct_droit_ptacs %}
            <tr>
                <td>{{ ct_droit_ptac.id }}</td>
                <td>{{ ct_droit_ptac.dpPrixMin }}</td>
                <td>{{ ct_droit_ptac.dpPrixMax }}</td>
                <td>{{ ct_droit_ptac.dpDroit }}</td>
                <td>
                    <a href="{{ path('ct_droit_ptac_show', {'id': ct_droit_ptac.id}) }}">voir</a>
                    <a href="{{ path('ct_droit_ptac_edit', {'id': ct_droit_ptac.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_droit_ptac_new') }}">Créer</a>
{% endblock %}
