{% extends 'base.html.twig' %}

{% block title %}CtDroitPtac{% endblock %}

{% block body %}
    <h1>Droit PTAC</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_droit_ptac.id }}</td>
            </tr>
            <tr>
                <th>Prix Minimum</th>
                <td>{{ ct_droit_ptac.dpPrixMin }}</td>
            </tr>
            <tr>
                <th>Prix Maximum</th>
                <td>{{ ct_droit_ptac.dpPrixMax }}</td>
            </tr>
            <tr>
                <th>Droit</th>
                <td>{{ ct_droit_ptac.dpDroit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_droit_ptac_index') }}">Retour</a>

    <a href="{{ path('ct_droit_ptac_edit', {'id': ct_droit_ptac.id}) }}">Modifier</a>

    {{ include('ct_droit_ptac/_delete_form.html.twig') }}
{% endblock %}
