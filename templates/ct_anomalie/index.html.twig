{% extends 'base.html.twig' %}

{% block title %}CtAnomalie index{% endblock %}

{% block body %}
    <h1>Liste Anomalie</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Anomalie Libelle</th>
                <th>Anomalie Code</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_anomalie in ct_anomalies %}
            <tr>
                <td>{{ ct_anomalie.id }}</td>
                <td>{{ ct_anomalie.anmlLibelle }}</td>
                <td>{{ ct_anomalie.anmlCode }}</td>
                <td>
                    <a href="{{ path('ct_anomalie_show', {'id': ct_anomalie.id}) }}">voir</a>
                    <a href="{{ path('ct_anomalie_edit', {'id': ct_anomalie.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_anomalie_new') }}">Créer</a>
{% endblock %}
