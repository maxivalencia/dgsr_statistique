{% extends 'base.html.twig' %}

{% block title %}CtAnomalie{% endblock %}

{% block body %}
    <h1>Anomalie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_anomalie.id }}</td>
            </tr>
            <tr>
                <th>Anomalie Libelle</th>
                <td>{{ ct_anomalie.anmlLibelle }}</td>
            </tr>
            <tr>
                <th>Anomalie Code</th>
                <td>{{ ct_anomalie.anmlCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_anomalie_index') }}">Retour</a>

    <a href="{{ path('ct_anomalie_edit', {'id': ct_anomalie.id}) }}">Modifier</a>

    {{ include('ct_anomalie/_delete_form.html.twig') }}
{% endblock %}
