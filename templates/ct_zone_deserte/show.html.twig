{% extends 'base.html.twig' %}

{% block title %}CtZoneDeserte{% endblock %}

{% block body %}
    <h1>Zone Deserte</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_zone_deserte.id }}</td>
            </tr>
            <tr>
                <th>Zone desserte Libelle</th>
                <td>{{ ct_zone_deserte.zdLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_zone_deserte_index') }}">Retour</a>

    <a href="{{ path('ct_zone_deserte_edit', {'id': ct_zone_deserte.id}) }}">Modifier</a>

    {{ include('ct_zone_deserte/_delete_form.html.twig') }}
{% endblock %}
