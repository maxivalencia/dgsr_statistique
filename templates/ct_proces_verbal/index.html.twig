{% extends 'base.html.twig' %}

{% block title %}CtProcesVerbal index{% endblock %}

{% block body %}
    <h1>Liste Proces Verbal</h1>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Pv Type</th>
                <th>Pv Tarif</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_proces_verbal in ct_proces_verbals %}
            <tr>
                <td>{{ ct_proces_verbal.id }}</td>
                <td>{{ ct_proces_verbal.pvType }}</td>
                <td>{{ ct_proces_verbal.pvTarif }}</td>
                <td>
                    <a href="{{ path('ct_proces_verbal_show', {'id': ct_proces_verbal.id}) }}">voir</a>
                    <a href="{{ path('ct_proces_verbal_edit', {'id': ct_proces_verbal.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_proces_verbal_new') }}">Créer</a>
{% endblock %}
