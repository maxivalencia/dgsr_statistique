{% extends 'base.html.twig' %}

{% block title %}CtProcesVerbal{% endblock %}

{% block body %}
    <h1>Proces Verbal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_proces_verbal.id }}</td>
            </tr>
            <tr>
                <th>Pv Type</th>
                <td>{{ ct_proces_verbal.pvType }}</td>
            </tr>
            <tr>
                <th>Pv Tarif</th>
                <td>{{ ct_proces_verbal.pvTarif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_proces_verbal_index') }}">Retour</a>

    <a href="{{ path('ct_proces_verbal_edit', {'id': ct_proces_verbal.id}) }}">Modifier</a>

    {{ include('ct_proces_verbal/_delete_form.html.twig') }}
{% endblock %}
