{% extends 'base.html.twig' %}

{% block title %}Edit CtVehicule{% endblock %}

{% block body %}
    <h1>Modification Véhicule</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    {{ include('ct_vehicule/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('ct_vehicule_index') }}">Retour</a>

    {{ include('ct_vehicule/_delete_form.html.twig') }}
{% endblock %}
