{% extends 'base.html.twig' %}

{% block title %}CtVisiteExtraTarif{% endblock %}

{% block body %}
    <h1>CtVisiteExtraTarif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_visite_extra_tarif.id }}</td>
            </tr>
            <tr>
                <th>VetAnnee</th>
                <td>{{ ct_visite_extra_tarif.vetAnnee }}</td>
            </tr>
            <tr>
                <th>VetPrix</th>
                <td>{{ ct_visite_extra_tarif.vetPrix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_visite_extra_tarif_index') }}">back to list</a>

    <a href="{{ path('ct_visite_extra_tarif_edit', {'id': ct_visite_extra_tarif.id}) }}">edit</a>

    {{ include('ct_visite_extra_tarif/_delete_form.html.twig') }}
{% endblock %}
