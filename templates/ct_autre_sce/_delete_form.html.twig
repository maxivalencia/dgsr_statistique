<form method="post" action="{{ path('ct_autre_sce_delete', {'id': ct_autre_sce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_autre_sce.id) }}">
    <button class="btn">Delete</button>
</form>
