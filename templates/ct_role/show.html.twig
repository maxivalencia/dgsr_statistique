{% extends 'base.html.twig' %}

{% block title %}CtRole{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_role.id }}</td>
            </tr>
            <tr>
                <th>Nom Rôle</th>
                <td>{{ ct_role.roleName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_role_index') }}">Retour</a>

    <a href="{{ path('ct_role_edit', {'id': ct_role.id}) }}">Modifier</a>

    {{ include('ct_role/_delete_form.html.twig') }}
{% endblock %}
