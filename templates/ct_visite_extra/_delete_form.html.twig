<form method="post" action="{{ path('ct_visite_extra_delete', {'id': ct_visite_extra.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet element?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_visite_extra.id) }}">
    <button class="btn">Supprimer</button>
</form>
