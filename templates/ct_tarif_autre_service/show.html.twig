{% extends 'base.html.twig' %}

{% block title %}CtTarifAutreService{% endblock %}

{% block body %}
    <h1>CtTarifAutreService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_tarif_autre_service.id }}</td>
            </tr>
            <tr>
                <th>TasTarif</th>
                <td>{{ ct_tarif_autre_service.tasTarif }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_tarif_autre_service_index') }}">back to list</a>

    <a href="{{ path('ct_tarif_autre_service_edit', {'id': ct_tarif_autre_service.id}) }}">edit</a>

    {{ include('ct_tarif_autre_service/_delete_form.html.twig') }}
{% endblock %}
