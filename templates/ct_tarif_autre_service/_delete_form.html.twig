<form method="post" action="{{ path('ct_tarif_autre_service_delete', {'id': ct_tarif_autre_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_tarif_autre_service.id) }}">
    <button class="btn">Delete</button>
</form>
