{% extends 'base.html.twig' %}

{% block title %}CtGenre index{% endblock %}

{% block body %}
    <h1>Liste Genre</h1>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Genre Libelle</th>
                <th>Genre Code</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_genre in ct_genres %}
            <tr>
                <td>{{ ct_genre.id }}</td>
                <td>{{ ct_genre.grLibelle }}</td>
                <td>{{ ct_genre.grCode }}</td>
                <td>
                    <a href="{{ path('ct_genre_show', {'id': ct_genre.id}) }}">voir</a>
                    <a href="{{ path('ct_genre_edit', {'id': ct_genre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun enregistrement trouver</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_genre_new') }}">Créer</a>
{% endblock %}
