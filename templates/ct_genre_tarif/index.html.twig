{% extends 'base.html.twig' %}

{% block title %}CtGenreTarif index{% endblock %}

{% block body %}
    <h1>CtGenreTarif index</h1>

    <table id="user_liste" class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>GrtPrix</th>
                <th>GrtAnnee</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ct_genre_tarif in ct_genre_tarifs %}
            <tr>
                <td>{{ ct_genre_tarif.id }}</td>
                <td>{{ ct_genre_tarif.grtPrix }}</td>
                <td>{{ ct_genre_tarif.grtAnnee }}</td>
                <td>
                    <a href="{{ path('ct_genre_tarif_show', {'id': ct_genre_tarif.id}) }}">show</a>
                    <a href="{{ path('ct_genre_tarif_edit', {'id': ct_genre_tarif.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('ct_genre_tarif_new') }}">Create new</a>
{% endblock %}
