<form method="post" action="{{ path('ct_carte_grise_delete', {'id': ct_carte_grise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_carte_grise.id) }}">
    <button class="btn">Delete</button>
</form>
