{% extends 'base.html.twig' %}

{% block title %}CtTypeVisite{% endblock %}

{% block body %}
    <h1>Type de Visite</h1>
    <div  align="center"><a href="{{ path('acceuil') }}" class="btn btn-success"><i class="fa fa-book" aria-hidden="true"> Acceuil</i></a></div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ct_type_visite.id }}</td>
            </tr>
            <tr>
                <th>Type visite Libelle</th>
                <td>{{ ct_type_visite.tpvLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ct_type_visite_index') }}">Retour</a>

    <a href="{{ path('ct_type_visite_edit', {'id': ct_type_visite.id}) }}">Modifier</a>

    {{ include('ct_type_visite/_delete_form.html.twig') }}
{% endblock %}
