<form method="post" action="{{ path('ct_type_visite_delete', {'id': ct_type_visite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ct_type_visite.id) }}">
    <button class="btn">Delete</button>
</form>
